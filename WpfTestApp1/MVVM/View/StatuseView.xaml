<UserControl x:Class="WpfTestApp1.MVVM.View.StatuseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:Model="clr-namespace:WpfTestApp1.MVVM.Model"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfTestApp1.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid  x:Name="root">

        <ItemsControl ItemsSource="{Binding A}"  Background="Transparent" Padding="12">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal"  
                                Width="450" 
                                Background="#22202F"
                                Height="45"
                                Margin="4" HorizontalAlignment="Left">
                        
                        <Label Content="{Binding Heading}" VerticalAlignment="Center"
                               FontSize="16"  HorizontalAlignment="Center"
                               Foreground="WhiteSmoke"
                                 Width="360" />

                        <Label Content="{Binding BudgetAmount}" VerticalAlignment="Center"
                               FontSize="14" Foreground="Orange" HorizontalAlignment="Center"
                                Width="45"/>

                        <Label Content="{Binding StatusAmount}" VerticalAlignment="Center"
                               FontSize="14" Foreground="GreenYellow" HorizontalAlignment="Center"
                                Width="45"/>
                       
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Grid>
</UserControl>
